1.cd = to enter into drectory
 2.ls = list of files
 3.ls -la = list the all files with ful data
 4 ls -lrt = list the all files in a row
 5 mkdir = create new directory
 6 touch = create new file
 7 rm = remove files
 8 rm -rf = remove directories
 9 cd .. = moves one step back
 10 pwd = show the path
 11 ps -cf = check the processor configuration
 12 df -h = show the disc size
 13 du -dh = show the storage of each files
 14 mkdir -p = create nested directories
 15 touch f name{} = create no of files at a time
 16 chmod -R = to give permissions
 17 vi = edit the file
 18 cat = read the file
 19 rm -rf * = remove the all files at a time
 20 cp = copy the file
 21 mv = move the file
 22 mv f nme name = rename the file
 23 nano = edit the file
 24 vim = edit the file
 25 apt get = install the packages
 26 apt-get install apache2 = install the apache2
 27 apt-get install tomcat7 = install the tomcat
 28 apt-get update = updte the all packages
 29 apt-get ufw allow 80 = set the port
 30 service apache2 start = start the apache server
 31 service apache status = check the status
 32 service apache2 stop = stop the apache2 server
 33 service apache 2 restart = restart teh server
 34 service tomcat start =tomcat start
 35 service tomcat stop = stop the tomcat server
 36 apt-get ufw allow 8080 = allow tom cat port
 37 cd /etc/apche2/ = apache2 conff files 
 38 cd var/apche2/www = go to html file
 39 cd html && touch index.html = enter the data into index.html file
 40 cd var/lib/tomcat7/webapps/ROOT = to depploy the file into tomcat